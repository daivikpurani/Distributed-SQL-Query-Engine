package com.distributed.sql.common.models;

/**
 * SQL operators for WHERE conditions
 */
public enum Operator {
    EQUALS,
    NOT_EQUALS,
    GREATER_THAN,
    LESS_THAN,
    GREATER_THAN_EQUALS,
    LESS_THAN_EQUALS,
    LIKE,
    IN
}
